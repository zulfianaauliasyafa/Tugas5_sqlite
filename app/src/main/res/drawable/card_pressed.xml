<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/darker_gray" /> <!-- Ganti dengan warna saat terklik -->
    <corners android:radius="8dp" /> <!-- Radius sudut -->
    <padding android:left="8dp" android:right="8dp" android:top="8dp" android:bottom="8dp"/>
    <stroke android:width="2dp" android:color="@android:color/holo_red_dark"/> <!-- Warna outline saat terklik -->
</shape>
